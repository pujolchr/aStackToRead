//prototype.js
// pujolchr AT gmiail.com


var Paper = {
    name: "nom",
    url: "https://naga.no",
    dateIn : ""
};

var stack = [];

function pushToRead(stack, paper) {
    return stack.unshift(paper);
}
function appendToRead(stack, paper) {
    return stack.push(paper);
}

function popToRead(stack, paper) {
    return 
}
